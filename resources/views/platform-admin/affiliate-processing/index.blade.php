@extends('platform-admin.layouts.app')
@section('title','Affiliate processing')
@section('heading','Affiliate processing approvals')
@section('content')
<div class="space-y-4">@if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
@foreach($requests as $item)<div class="rounded-2xl border bg-white p-5 shadow-sm"><div class="flex flex-wrap justify-between gap-4"><div><h3 class="font-semibold">{{ $item->affiliate->name }}</h3><p class="text-sm text-slate-500">{{ $item->parentBusiness->name }} · {{ str($item->requested_management_mode)->replace('_',' ')->title() }} · {{ str($item->requested_processing_engine)->replace('_',' ')->title() }}</p><span class="mt-2 inline-block rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize">{{ $item->status }}</span></div>@if($item->status==='pending')<div class="flex gap-2"><form method="POST" action="{{ route('platform-admin.affiliate-processing.review',$item) }}">@csrf @method('PATCH')<input type="hidden" name="decision" value="approved"><button class="rounded-lg bg-emerald-600 px-3 py-2 text-sm font-semibold text-white">Approve</button></form><form method="POST" action="{{ route('platform-admin.affiliate-processing.review',$item) }}" class="flex gap-2">@csrf @method('PATCH')<input type="hidden" name="decision" value="rejected"><input name="rejection_reason" required placeholder="Rejection reason" class="rounded-lg border-slate-200 text-sm"><button class="rounded-lg bg-red-600 px-3 py-2 text-sm font-semibold text-white">Reject</button></form></div>@endif</div></div>@endforeach<div>{{ $requests->links() }}</div></div>
@endsection
