@extends('parent-admin.layouts.app')
@section('title','Settlement wallets')
@section('heading','Settlement wallets')
@section('content')
<section class="rounded-2xl border bg-white shadow-sm"><div class="border-b p-5"><h2 class="font-semibold">Affiliate business balances</h2><p class="mt-1 text-sm text-slate-500">Fund parent-managed affiliates and review each affiliate processing mode.</p></div><div class="divide-y">@forelse($affiliates as $affiliate)<div class="flex flex-wrap items-center justify-between gap-4 p-5"><div><p class="font-semibold">{{ $affiliate->name }}</p><p class="text-xs text-slate-500">{{ $affiliate->processingProfile ? str($affiliate->processingProfile->management_mode)->replace('_',' ')->title() : 'Not initialized' }}</p></div><div class="text-right"><p class="font-bold">₦{{ number_format((float)($affiliate->settlementWallet?->available_balance ?? 0),2) }}</p><p class="text-xs text-slate-500">Reserved ₦{{ number_format((float)($affiliate->settlementWallet?->reserved_balance ?? 0),2) }}</p><a href="{{ route('parent-admin.affiliates.settlement-wallet.show',$affiliate) }}" class="mt-2 inline-block rounded-lg border px-3 py-2 text-sm font-semibold">Manage wallet & processing</a></div></div>@empty<div class="p-8 text-center text-slate-500">No affiliates assigned yet.</div>@endforelse</div><div class="p-4">{{ $affiliates->links() }}</div></section>
@endsection
